1 git clone https://github.com/Mercury7z/vending_machine.git
2 git add подсвеченные файлы
я не хочу каждый коммит описывать так как по сути буду повторяться так что тут формула на все коммиты
3 git commit -m комментарии к комиту
связывать я с удаленным репозиторием не стал так как у видимо из за того что я клонировал он связался автоматически
гит инит писать тоже не пришлось
4 git push origin home_work_25
5 git log смотрю локальные коммиты щас уже все сделал
6 git merge я слил хоумворк в мастера сделал как на уроке так как не знал будет ли у меня удобный интерфейс для разрешения конфликтов или нет